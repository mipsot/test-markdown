 Command          | Result                            | Control (should be empty)
------------------|-----------------------------------|--------------
 `ls -1|grep test`| List files contains `test` in name|
